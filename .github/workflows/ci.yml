name: CI Pull Request
on:
  # Ejecución automática en PRs
  pull_request:
    branches: [ "main" ]
  
  # Ejecución Manual (El botón "Run workflow")
  workflow_dispatch:

jobs:
  call-governance-pipeline:
    # Recuerda cambiar 'valentechy' por tu usuario
    uses: valentechy/GitHub-Actions-Gobernanza/.github/workflows/ci-pr-pipeline.yml@main
    secrets: inherit
